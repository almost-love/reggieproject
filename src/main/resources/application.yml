server:
  port: 8080
spring:
  application:
    #应用的名称，可选
    name: reggie
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://192.168.207.128:3306/reggie?useSSL=false
      username: root
      password: root

  mail:
    username: 481393117@qq.com
    password: orvsdslozyrrbjcg
    host: smtp.qq.com
    properties:
      properties:
        mail:
          smtp:
            ssl:
              enable: true
    default-encoding: utf-8
    server.port: 8085
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
reggie:
  path: D:\img\


